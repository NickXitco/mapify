class Vertex {
    x;
    y;

    size;
    color;

    quad;

    visible;

    constructor(x, y, size, color) {
        this.x = x;
        this.y = y;
        this.size = size;
        this.color = color;
    }
}
